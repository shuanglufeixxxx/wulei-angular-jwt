import { MenubarItemDirective } from './menubar-item.directive';

describe('MenubarItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MenubarItemDirective();
    expect(directive).toBeTruthy();
  });
});
