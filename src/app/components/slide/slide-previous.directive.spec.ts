import { SlidePreviousDirective } from './slide-previous.directive';

describe('SlidePreviousDirective', () => {
  it('should create an instance', () => {
    const directive = new SlidePreviousDirective();
    expect(directive).toBeTruthy();
  });
});
