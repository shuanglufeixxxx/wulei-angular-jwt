import { SlideNextDirective } from './slide-next.directive';

describe('SlideNextDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideNextDirective();
    expect(directive).toBeTruthy();
  });
});
